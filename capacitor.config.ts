import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Learning to Create Vue App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
